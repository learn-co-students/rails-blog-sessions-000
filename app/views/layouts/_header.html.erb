
<!-- This current user is also a method references in the application controller CHECK!! -->
 <% if current_user %>
 <a href="/users/<%=current_user.id%>"><%= "Hello, #{current_user.name}!" %></a>
 <%= link_to "Log Out", logout_path, method: :delete %>
<%= link_to 'New Post', new_post_path %>
<% else %>
<%= link_to "Sign Up", signup_path %>
<%= link_to "Log In", login_path %>
<%end%>

<!-- I also didn't seem to add the entire header but a lot of that was just better view. -->