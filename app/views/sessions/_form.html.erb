<%= form_tag sessions_path do %>
  <div class="field">
    <%= label_tag :email %><br />
    <%= text_field_tag :email, params[:email] %>
  </div>
  <div class="field">
    <%= label_tag :password %><br />
    <%= password_field_tag :password, nil %>
  </div>
  <div class="actions"><%= submit_tag "Log In" %></div>
<% end %>

<!-- Had to add this entire part and nothing seemed to be working w/o it definitely need to check what's going on here... CHECK!! -->